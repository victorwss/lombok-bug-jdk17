plugins {
    id "java-library"
}

apply(plugin: "java-library")

dependencies {
    annotationProcessor(group: "org.projectlombok", name: "lombok", version: "1.18.20")
    compileOnlyApi(group: "org.projectlombok", name: "lombok", version: "1.18.20")
}

repositories {
    mavenCentral()
}
